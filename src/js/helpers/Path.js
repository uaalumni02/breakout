const BASE = '/api';


const LOGIN = `${ BASE }/user/login`;
const SIGNUP = `${ BASE }/user/signup`;

const PROFILE = `${ BASE }/user`;

const MEDIA = `${ BASE }/media`;
const COMMENT = `${ BASE }/comment`;
const RATE = `${ BASE }/rate`;


export {
    COMMENT,
    LOGIN,
    MEDIA,
    PROFILE,
    RATE,
    SIGNUP,
}


5/5

4.5
